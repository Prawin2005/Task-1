
let Timer = 60;
let quizTimer;

setTimeout(function () {
    quizTimer = setInterval(fun);
}, 1000); 

function fun() {
    console.log("Timer:", Timer);

    Timer -= 1;

    if (Timer == 0) {
        clearInterval(quizTimer);
        console.log("time up");
    }
}
